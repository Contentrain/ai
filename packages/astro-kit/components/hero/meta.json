{
  "id": "hero",
  "name": "Hero",
  "description": "The page's opening section: optional eyebrow, heading, lead, action buttons and an image — beside the text, below it, or behind it as a full-bleed cover with a scrim.",
  "category": "section",
  "uses": [],
  "shared": [
    "Button.astro",
    "KitImage.astro",
    "Section.astro",
    "types.ts"
  ],
  "variants": {
    "layout": {
      "options": [
        "split",
        "centered",
        "cover"
      ],
      "default": "split",
      "description": "split: text and image side by side (stacked on phones). centered: centred text with the image below. cover: the image fills the section behind the text, darkened by a scrim; without an image it falls back to split."
    },
    "tone": {
      "options": [
        "default",
        "muted",
        "inverse",
        "accent"
      ],
      "default": "default",
      "description": "Section background (ignored by cover, which is always light text on the image)."
    },
    "align": {
      "options": [
        "start",
        "center"
      ],
      "default": "center",
      "description": "Text position over a cover image (layout cover only): centred, as WordPress covers default to, or at the start edge."
    }
  },
  "props": {
    "heading": {
      "type": "string",
      "label": "Heading",
      "required": true,
      "description": "The main heading (Cover/Group heading, Elementor heading, Divi header title)."
    },
    "level": {
      "type": "select",
      "options": [
        "1",
        "2"
      ],
      "default": "1",
      "content": false,
      "description": "Heading level: 1 when the hero opens the page, 2 when the page already has an h1."
    },
    "eyebrow": {
      "type": "string",
      "label": "Eyebrow",
      "description": "Short line above the heading."
    },
    "lead": {
      "type": "text",
      "label": "Lead",
      "description": "Paragraph under the heading."
    },
    "actions": {
      "type": "array",
      "label": "Buttons",
      "items": {
        "type": "object",
        "fields": {
          "label": {
            "type": "string",
            "label": "Label",
            "required": true,
            "description": "The button text."
          },
          "href": {
            "type": "url",
            "label": "Link",
            "required": true,
            "description": "Where the button goes: a page on this site or a full address."
          },
          "newTab": {
            "type": "boolean",
            "label": "Open in a new tab",
            "description": "Open the link in a new tab (an external site the reader should not leave for)."
          },
          "style": {
            "type": "select",
            "label": "Style",
            "options": [
              "primary",
              "secondary",
              "ghost"
            ],
            "description": "primary for the main action, secondary or ghost for the ones beside it."
          }
        }
      },
      "description": "Buttons, in order."
    },
    "image": {
      "type": "object",
      "label": "Image",
      "fields": {
        "src": {
          "type": "image",
          "label": "Image",
          "required": true,
          "description": "The image file."
        },
        "alt": {
          "type": "string",
          "label": "Image description",
          "required": true,
          "description": "What the image shows, for readers who cannot see it; empty when it is decoration."
        },
        "width": {
          "type": "integer",
          "label": "Width",
          "description": "Width in pixels, as uploaded."
        },
        "height": {
          "type": "integer",
          "label": "Height",
          "description": "Height in pixels, as uploaded."
        }
      },
      "description": "Hero image; loaded first (the page's largest image)."
    },
    "id": {
      "type": "string",
      "content": false,
      "description": "Anchor id of the section; the heading gets `<id>-title`."
    }
  },
  "slots": {},
  "js": "none",
  "dependencies": {},
  "sources": {
    "gutenberg": [
      "core/cover",
      "core/media-text"
    ],
    "divi": [
      "divi/et_pb_fullwidth_header",
      "divi/et_pb_fullwidth_image"
    ],
    "classic": [
      "classic/hero"
    ]
  },
  "a11y": "A labelled region (aria-labelledby the heading). One h1 by default; `level: 2` inside pages that already have one. The cover image is decorative when its alt is empty; text sits on an ink scrim for contrast."
}
