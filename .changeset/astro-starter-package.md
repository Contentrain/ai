---
'@contentrain/astro-starter': minor
---

New package. `@contentrain/astro-starter` publishes the Astro + Contentrain starter so that tools outside this monorepo can copy it. The migration's site writer is one of them. `STARTER_DIR` is the directory holding the starter; the template itself is packed from `templates/astro-starter`.
