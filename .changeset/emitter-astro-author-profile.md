---
'@contentrain/emitter-astro': minor
---

Authors as one person across the site. `EmitPost.author_url` (the author's archive) becomes the Article author's `url`; `QueryPage.profile` makes the author's archive a `ProfilePage` whose main entity is a `Person` at that same address, with bio, image and `sameAs`.
