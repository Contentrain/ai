# Contentrain AI Documentation

> Contentrain AI is the MIT-licensed open-source toolkit for AI-generated content governance: a 27-tool MCP server (@contentrain/mcp), a CLI (contentrain), shared TypeScript types (@contentrain/types), agent rules and skills (@contentrain/rules, @contentrain/skills), a generated typed query SDK (@contentrain/query), and the open ends of the WordPress migration pipeline (@contentrain/wp-import, @contentrain/emitter-astro, @contentrain/verify). Content lives as canonical JSON/Markdown in your Git repo under .contentrain/ — agents write through deterministic MCP tools, humans review through git branches.

Full documentation as a single file (for LLMs): https://ai.contentrain.io/llms-full.txt
Contentrain Studio (team web surface) documentation: https://docs.contentrain.io/llms.txt

## Packages (npm)

- @contentrain/mcp — MCP server: 27 deterministic tools (22 core + 5 media), stdio + HTTP transports, Local / GitHub / GitLab providers
- contentrain — CLI: init, import, serve (review UI + MCP transports), validate, generate, diff, merge, reconcile, prune, studio commands
- @contentrain/query — universal query SDK; `contentrain generate` emits a typed client into .contentrain/client/ (#contentrain imports); optional CDN transport plus public forms/comments clients
- @contentrain/types — shared contract: model/field/config/provider types, the migration contracts (RawIR, ProjectIR, CapabilityManifest, MigrationHandoff), plus pure, browser-safe validate/serialize functions
- @contentrain/rules — always-loaded agent guardrails plus shared quality rules
- @contentrain/skills — 16 on-demand Agent Skills (Agent Skills standard) and 9 framework guides
- @contentrain/wp-import — WordPress WXR/REST importers → RawIR → .contentrain content store
- @contentrain/emitter-astro — ProjectIR → a complete Astro project, returned as a pure file map
- @contentrain/verify — offline SEO/indexing/link/asset gates for a built site; takes documents, never makes a network request

## Key Facts

- 4 model kinds: collection (object-map by entry id), singleton, dictionary (flat string map), document (markdown + frontmatter)
- Every model declares title_field: the field shown as an entry's title. Must be a string/text/slug/email/url/code/markdown/richtext field on that model; dictionaries use the reserved "key". Required — `contentrain validate --fix` backfills it in older projects
- 27 field types; content is validated before every write — an error on any entry in a contentrain_content_save call means nothing is committed
- The MCP tool registry holds 27 tools, but `tools/list` is filtered per session: a tool whose requirements (local project root, provider capabilities, a media stack) cannot be met is never registered. A local stdio server lists the 22 core tools; media tools appear only on media-capable providers such as Studio MCP Cloud
- Every write lands on an isolated cr/* git branch; workflow is auto-merge or review (config.json)
- dry_run preview belongs to contentrain_apply (the normalize tool that can patch source); destructive tools require confirm: true
- Locale strategies: file / suffix / directory / none; entry status and scheduling live in .contentrain/meta/, not in content
- When a change lands on the base branch directly, the branches diverge: writes still succeed but report base_advance "blocked_diverged". contentrain_reconcile / `contentrain reconcile` is a content-aware three-way merge (field, dictionary key, term+locale granularity) with a closed 9-value conflict code set
- WordPress migration chain: wp-import → RawIR → [migration engine, CLOSED SOURCE] → ProjectIR → emitter-astro → verify. The MIT packages are the two ends and the contracts; the analysis that produces a ProjectIR is not in this repository and these packages do not convert a site on their own
- MIT licensed, local-first (no cloud dependency); GitHub/GitLab remote providers are optional peer dependencies

## Key Pages

- Getting Started: /getting-started
- 2-Minute Demo: /demo
- Ecosystem Map: /ecosystem
- Core Concepts: /concepts
- Contentrain Studio: /studio
- MCP Tools: /packages/mcp
- CLI: /packages/cli
- Query SDK: /packages/sdk
- Rules: /packages/rules
- Skills: /packages/skills
- Types: /packages/types
- WordPress Import: /packages/wp-import
- Astro Emitter: /packages/emitter-astro
- Verify: /packages/verify
- Your First Model (guided walkthrough): /guides/first-model
- Providers & Transports: /guides/providers
- HTTP Transport: /guides/http-transport
- Embedding MCP: /guides/embedding-mcp
- Normalize Flow: /guides/normalize
- Framework Integration: /guides/frameworks
- i18n Workflow: /guides/i18n
- Serve UI: /guides/serve-ui
- WordPress Migration: /guides/migration
- Forms & Comments: /guides/forms-comments
- Model Kinds: /reference/model-kinds
- Field Types: /reference/field-types
- Configuration: /reference/config
- RepoProvider Reference: /reference/providers
- Reconcile & Divergence: /reference/reconcile
